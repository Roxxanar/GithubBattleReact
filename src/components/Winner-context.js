import { createContext } from "react";

// Create a context with an initial value (null in this case)
export const WinnerContext = createContext(null);

// function displayWinnerContext ()
// {
// return (
// <WinnerContext.Provider value={Winner}>
//       {children}
//     </WinnerContext.Provider>
// );

// }

// export default displayWinnerContext;